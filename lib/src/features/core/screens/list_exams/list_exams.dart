import 'package:flutter/material.dart';

class ListExamsScreen extends StatelessWidget {
  const ListExamsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "List Exams"
      ),
    );
  }
}
